appId: com.conuwalks.app
---
- launchApp:
    clearState: true
    permissions:
      all: allow
- runFlow: 
    file: ../subflows/setup.yaml
    env:
        USERNAME: "${USERNAME}"
        PASSWORD: "${PASSWORD}"
